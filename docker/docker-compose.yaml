services:
  php:
    build: php-fpm
    ports:
      - '9000:9000'
    volumes:
      - ../application:/var/www/application:cached
  nginx:
    build: nginx
    ports:
      - '8080:80'
    links:
      - php
    volumes_from:
      - php